global:
  resolve_timeout: 5m

route:
  receiver: local-webhook
  group_by:
    - alertname
    - job
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 5m

receivers:
  - name: local-webhook
    webhook_configs:
      - url: http://alert-webhook:8080/
        send_resolved: true
