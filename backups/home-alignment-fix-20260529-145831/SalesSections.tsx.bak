"use client";

import Container from "@/components/layout/Container";
import SectionTitle from "@/components/ui/SectionTitle";
import Button from "@/components/ui/Button";
import { useLanguage } from "@/i18n/LanguageProvider";

const whyItems = [
  {
    titleTr: "Canlı görerek karar",
    title: "Decide by seeing live",
    descriptionTr:
      "Müşteri sadece açıklama okumaz; çalışan örnekleri gezerek sistemin mantığını görebilir.",
    description:
      "Clients do not only read descriptions; they can review working examples and understand the system."
  },
  {
    titleTr: "Mobil uyumlu yapı",
    title: "Mobile-friendly structure",
    descriptionTr:
      "Site ve paneller telefon, tablet ve bilgisayarda rahat kullanılacak şekilde düzenlenir.",
    description:
      "Websites and panels are arranged to work comfortably on phones, tablets and desktops."
  },
  {
    titleTr: "Net kapsam",
    title: "Clear scope",
    descriptionTr:
      "Başlamadan önce sayfa, modül, teslim kapsamı ve ekstra istekler açıkça konuşulur.",
    description:
      "Pages, modules, delivery scope and extra requests are clarified before starting."
  },
  {
    titleTr: "Güvenli ilerleme",
    title: "Secure process",
    descriptionTr:
      "İsterseniz süreç Bionluk üzerinden özel teklif veya paket mantığıyla ilerletilebilir.",
    description:
      "If preferred, the process can continue through Bionluk with a custom offer or package."
  }
];

const processItems = [
  {
    titleTr: "İhtiyacınızı yazarsınız",
    title: "You share your need",
    descriptionTr: "İşletme türünüz, istediğiniz sistem ve varsa beğendiğiniz canlı örnek konuşulur.",
    description: "Your business type, desired system and any live example you liked are discussed."
  },
  {
    titleTr: "Uygun örnek seçilir",
    title: "A suitable example is selected",
    descriptionTr: "Size en yakın canlı örnek üzerinden nelerin değişeceği netleştirilir.",
    description: "The closest live example is used to clarify what needs to change."
  },
  {
    titleTr: "Kapsam belirlenir",
    title: "Scope is defined",
    descriptionTr: "Sayfa sayısı, admin panel, ödeme, randevu, katalog veya özel modüller belirlenir.",
    description: "Page count, admin panel, payment, appointment, catalog or custom modules are defined."
  },
  {
    titleTr: "Önizleme paylaşılır",
    title: "Preview is shared",
    descriptionTr: "Uygun projelerde teslim öncesi kontrol edilebilir canlı önizleme paylaşılır.",
    description: "For suitable projects, a reviewable live preview is shared before delivery."
  },
  {
    titleTr: "Yayına alınır",
    title: "Project goes live",
    descriptionTr: "Onay sonrası domain, hosting ve yayın süreci hazırlanır.",
    description: "After approval, domain, hosting and publishing steps are prepared."
  }
];

const packages = [
  {
    titleTr: "Tanıtım Sitesi",
    title: "Business Website",
    itemsTr: ["Kurumsal web sitesi", "Landing page", "İletişim ve teklif akışı"],
    items: ["Corporate website", "Landing page", "Contact and quote flow"]
  },
  {
    titleTr: "Satış / Katalog Sistemi",
    title: "Sales / Catalog System",
    itemsTr: ["Ürün katalog", "WhatsApp sipariş", "E-ticaret başlangıcı"],
    items: ["Product catalog", "WhatsApp order", "E-commerce starter"]
  },
  {
    titleTr: "Özel Panel / Yazılım",
    title: "Custom Panel / Software",
    itemsTr: ["Admin panel", "Randevu / görev / stok", "Özel iş akışları"],
    items: ["Admin panel", "Appointment / task / stock", "Custom workflows"]
  }
];

export default function SalesSections() {
  const { language } = useLanguage();
  const isTr = language === "tr";

  return (
    <>
      <section className="py-12 sm:py-16 lg:py-20">
        <Container>
          <SectionTitle
            eyebrow={isTr ? "Neden NT Web Çözümleri?" : "Why NT Web Solutions?"}
            title={
              isTr
                ? "Dükkan sahibinin anlayacağı, kullanacağı ve güveneceği sistemler"
                : "Systems business owners can understand, use and trust"
            }
            description={
              isTr
                ? "Amaç sadece güzel görünen bir sayfa hazırlamak değil; müşterinin işini anlatan, talep toplayan ve gerektiğinde yönetilebilen bir yapı kurmaktır."
                : "The goal is not only a good-looking page, but a usable structure that explains the business, collects requests and can be managed when needed."
            }
          />

          <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 xl:grid-cols-4">
            {whyItems.map((item) => (
              <div
                key={item.titleTr}
                className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"
              >
                <h3 className="text-lg font-black leading-6 text-slate-950">
                  {isTr ? item.titleTr : item.title}
                </h3>
                <p className="mt-3 text-sm leading-7 text-slate-600">
                  {isTr ? item.descriptionTr : item.description}
                </p>
              </div>
            ))}
          </div>
        </Container>
      </section>

      <section className="py-12 sm:py-16 lg:py-20">
        <Container>
          <div className="grid gap-8 lg:grid-cols-[0.85fr_1.15fr] lg:items-start">
            <div>
              <SectionTitle
                eyebrow={isTr ? "Süreç" : "Process"}
                title={isTr ? "Nasıl ilerliyoruz?" : "How does the work move?"}
                description={
                  isTr
                    ? "Süreci teknik terimlere boğmadan, küçük ve anlaşılır adımlarla ilerletiyoruz."
                    : "We keep the process clear and split the work into understandable steps."
                }
              />
              <Button href="/contact" className="h-12 min-h-12 justify-center px-6 text-sm">
                {isTr ? "Projem İçin Teklif Al" : "Request a Quote"}
              </Button>
            </div>

            <div className="grid gap-3">
              {processItems.map((item, index) => (
                <div
                  key={item.titleTr}
                  className="flex gap-4 rounded-3xl border border-slate-200 bg-white p-4 shadow-sm sm:p-5"
                >
                  <span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-full bg-slate-950 text-sm font-black text-white">
                    {index + 1}
                  </span>

                  <div>
                    <h3 className="text-base font-black leading-6 text-slate-950">
                      {isTr ? item.titleTr : item.title}
                    </h3>
                    <p className="mt-2 text-sm leading-7 text-slate-600">
                      {isTr ? item.descriptionTr : item.description}
                    </p>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </Container>
      </section>

      <section className="py-12 sm:py-16 lg:py-20">
        <Container>
          <SectionTitle
            eyebrow={isTr ? "Proje Türleri" : "Project Types"}
            title={isTr ? "İhtiyaca göre başlangıç noktaları" : "Starting points by need"}
            description={
              isTr
                ? "Net fiyat yerine önce ihtiyaç belirlenir. Canlı örnek üzerinden kapsam konuşulur ve benzer sistem için teklif hazırlanır."
                : "Instead of forcing one fixed price, the need is clarified first. Scope is discussed through a live example and a quote is prepared for a similar system."
            }
          />

          <div className="grid grid-cols-1 gap-4 md:grid-cols-3">
            {packages.map((pkg) => (
              <div
                key={pkg.titleTr}
                className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"
              >
                <h3 className="text-xl font-black text-slate-950">
                  {isTr ? pkg.titleTr : pkg.title}
                </h3>

                <ul className="mt-4 space-y-3 text-sm leading-6 text-slate-600">
                  {(isTr ? pkg.itemsTr : pkg.items).map((item) => (
                    <li key={item}>- {item}</li>
                  ))}
                </ul>

                <p className="mt-5 rounded-2xl border border-emerald-200 bg-emerald-50 p-4 text-sm font-semibold leading-6 text-emerald-800">
                  {isTr
                    ? "Projeye göre fiyatlandırılır. Benzer sistem için teklif alın."
                    : "Priced by scope. Request a quote for a similar system."}
                </p>
              </div>
            ))}
          </div>
        </Container>
      </section>
    </>
  );
}
